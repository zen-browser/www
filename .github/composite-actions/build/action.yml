name: 'Build Project'
description: 'Build the project and upload build artifacts'

runs:
  using: 'composite'
  steps:
    - name: Build project
      shell: bash
      run: pnpm exec turbo run build

    - name: Upload build output
      uses: actions/upload-artifact@v4
      with:
        name: build
        path: |
          dist 